<div class="container" style="padding: 80px 24px; min-height: 60vh;">
  <h1 style="margin-bottom: 24px; font-size: 36px; color: var(--color-gold);">{{ page.title }}</h1>
  <div class="page-content" style="color: var(--color-text-sub); line-height: 1.8;">
    {{ page.content }}
  </div>
</div>
